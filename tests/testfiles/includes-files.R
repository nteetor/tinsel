#. sample-utils$ramp_to(5)
div_scale <- function() {
  cm.colors(3)
}

#. sample-utils$as_double
dbl_c <- `c`

#. sample-utils.R$as_double
boring <- function(m) {
  m * 30
}

#. r-function.utils$draw_box
smiley <- function() {
  expr <- ':^)'
  cat(noquote(expr))
}
